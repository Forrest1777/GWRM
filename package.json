{
  "name": "gwrm",
  "version": "1.1.0",
  "private": true,
  "description": "Godot Worktree Runtime Manager for Hermes, Docker, LSP, Godot MCP, GUT, and host-side Computer Use.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/launcher.mjs --config gwrm.config.json",
    "check": "node scripts/check-all.mjs",
    "test": "node --test --test-concurrency=1 tests/*.test.mjs"
  },
  "dependencies": {
    "@coding-solo/godot-mcp": "0.1.1",
    "mcp-proxy": "6.5.4"
  }
}
